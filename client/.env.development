# Development environment — loaded automatically when running: npx expo start
#
# EXPO_PUBLIC_API_URL is intentionally absent.
# networkUtils.js auto-detects the Metro bundler's host IP so the app
# connects to your local server without any manual configuration.
EXPO_PUBLIC_API_PORT=3000
